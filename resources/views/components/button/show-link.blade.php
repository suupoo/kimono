<a
    href="{{ $link }}"
    class="text-white bg-gray-500 hover:text-gray-500 hover:bg-white text-center border border-gray-500 font-bold py-2 px-4 rounded">
    <span class="hidden md:inline">{{ __('resource.show') }}</span>
    <span class="inline md:hidden">@include('components.button.icons.detail')</span>
</a>
