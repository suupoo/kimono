<a
    href="{{ $link }}"
    class="w-full bg-blue-500 text-white hover:text-blue-500 hover:bg-white border border-blue-500 font-bold py-2 px-4 rounded">
    <span class="hidden sm:inline">{{ __('resource.edit') }}</span>
    <span class="inline sm:hidden">@include('components.button.icons.edit')</span>
</a>
