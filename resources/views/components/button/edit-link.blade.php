<a
    href="{{ $link }}"
    class="w-full bg-blue-500 text-white hover:text-blue-500 hover:bg-white text-center border border-blue-500 font-bold py-2 px-4 rounded">
    <span class="hidden md:inline">{{ __('resource.edit') }}</span>
    <span class="inline md:hidden">@include('components.button.icons.edit')</span>
</a>
